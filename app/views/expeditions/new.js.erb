$(function () {
    if (!$('div.container').children().hasClass('simple_form new_expedition')) {
        $('.expedition-list').before('<%= escape_javascript(render('form')) %>')
    }
    else {
        console.log('class present')
    }
})